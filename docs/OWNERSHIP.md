# Integration ownership

ESP-Now-specific Event representations and adapters are owned by ESPressio ESP-Now because ESP-Now is already the downstream optional consumer of the generic ESPressio Event mechanism. This includes the concrete Event Transport, ESP-Now lifecycle Event types, and `ESPNowTransportEventBridge`.
